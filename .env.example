# MySQL (backend reads DB_URL, DB_USER, DB_PASS)
# Build URL from parts: jdbc:mysql://DB_HOST:DB_PORT/DB_NAME?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
DB_URL=jdbc:mysql://localhost:3306/comm2020_dpp?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
DB_USER=comm2020
DB_PASS=comm2020_password

# Optional: for scripts/migrations only
DB_HOST=localhost
DB_PORT=3306
DB_NAME=comm2020_dpp

# App
APP_PORT=8080
APP_ENV=local

# Auth (if using simple token/session)
AUTH_SECRET=change_me_in_real_env
